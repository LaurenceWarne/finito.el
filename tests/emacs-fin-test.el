;;; test-ox-yaow.el --- Tests for emacs-fin.el -*- lexical-binding: t -*-

;;; Commentary:

;; Tests for emacs-fin.el

;;; Code:

(require 'buttercup)
(require 'emacs-fin)

(describe "some test"
  (it "test foo"
    (expect (1+ 1) :to-equal 2)))
